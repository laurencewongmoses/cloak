<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cloak.history.dao.BaseHistoryDao" >
  <!--查询历史表：根据表名 -->
  <select id="findByTableName" statementType="STATEMENT" parameterType="string" resultType="com.cloak.stock.base.BaseStockHistory">
    SELECT * FROM ${tablename}
  </select>

  <select id="selectTableName" resultType="string" parameterType="string">
    SELECT table_name FROM information_schema.TABLES WHERE table_name = #{tablename};
  </select>
</mapper>

